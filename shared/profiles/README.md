# Fixture Profiles

JSON fixture profiles define channel mappings and behavior defaults.

## Notes

- Channel indices are 1-based DMX channel numbers.
- 16-bit attributes use `coarse` and `fine`.
- Keep profile IDs stable for config compatibility.
